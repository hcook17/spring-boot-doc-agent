# Development-only dependencies. Deliberately NOT in requirements.txt: that
# file is the plugin's runtime prerequisite set (ast-grep-cli, sqllineage,
# pathspec), documented as such in CONSTRAINTS.md's "Runtime prerequisites"
# item 4. Nothing here is needed to run the pipeline -- only to check the
# code that implements it -- and folding a linter into the runtime set would
# make that list say something untrue.
#
# Compatible-release pins, matching requirements.txt's convention: a fresh
# environment reliably gets a working version without being frozen forever.
#
# ruff rather than flake8 + black + isort: one binary covering all three,
# 48.8k stars, last release 0.16.0 on 2026-07-23 (checked against the GitHub
# API on 2026-07-24, not a blog post). Its C901 complexity rule is enabled in
# .ruff.toml but is NOT the ratchet -- see scripts/check_code_quality.py,
# which owns the committed per-function baseline ruff has no equivalent for.
ruff~=0.16.0
