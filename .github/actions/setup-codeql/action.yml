name: Setup CodeQL
description: >
  Download or reuse a pinned CodeQL bundle, verify its SHA256, and add it to
  PATH. Expects the workflow to set CODEQL_BUNDLE_URL and CODEQL_SHA256.

inputs:
  bundle-path:
    description: >
      Path to an existing codeql-bundle.tar.gz archive to use instead of
      downloading. If empty, the bundle is downloaded from CODEQL_BUNDLE_URL.
    required: false
    default: ""
  codeql-dir:
    description: Directory where CodeQL should be extracted.
    required: false
    default: ${{ runner.temp }}/codeql

outputs:
  codeql-dir:
    description: Directory where CodeQL was extracted.
    value: ${{ inputs.codeql-dir }}

runs:
  using: composite
  steps:
    - name: Download or reuse CodeQL bundle
      shell: bash
      env:
        BUNDLE_PATH: ${{ inputs.bundle-path }}
        CODEQL_DIR: ${{ inputs.codeql-dir }}
      run: |
        set -euo pipefail

        if [ -z "${CODEQL_BUNDLE_URL:-}" ]; then
          echo "ERROR: CODEQL_BUNDLE_URL is not set" >&2
          exit 1
        fi
        if [ -z "${CODEQL_SHA256:-}" ]; then
          echo "ERROR: CODEQL_SHA256 is not set" >&2
          exit 1
        fi

        mkdir -p "$CODEQL_DIR"
        BUNDLE="$CODEQL_DIR/codeql-bundle.tar.gz"
        if [ -n "$BUNDLE_PATH" ]; then
          cp "$BUNDLE_PATH" "$BUNDLE"
        else
          curl -fsSL -o "$BUNDLE" "$CODEQL_BUNDLE_URL"
        fi
        echo "$CODEQL_SHA256  $BUNDLE" | sha256sum -c -
        tar -xzf "$BUNDLE" -C "$CODEQL_DIR" --strip-components=1
        echo "$CODEQL_DIR" >> "$GITHUB_PATH"
        echo "Extracted CodeQL to $CODEQL_DIR"
