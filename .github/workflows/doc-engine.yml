name: doc-engine pipeline certification

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      repo_path:
        description: "Path to the repository to scan (relative to workspace)"
        required: true
        default: "."
      compliance_profile:
        description: "scan_only, deterministic_only, or certified"
        required: true
        default: "scan_only"

permissions:
  contents: read
  pull-requests: write

jobs:
  certify-scan-only:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          pip install -e .

      - name: Run pipeline (scan_only on fixture signals)
        run: |
          PROFILE="${{ github.event.inputs.compliance_profile || 'scan_only' }}"
          doc-engine pipeline run scripts/fixtures/spring_signals \
            --compliance-profile "$PROFILE" \
            --out-dir pipeline-artifacts-scan \
            --skip-drift \
            --signals-file scripts/fixtures/spring_signals_fixture_expected.json

      - name: Gate certification.json (scan_only)
        run: doc-engine certification verify --allow-mock pipeline-artifacts-scan/certification.json

  certify-certified:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
          pip install -e .

      - name: Run pipeline (certified mock on fixture signals)
        run: |
          doc-engine pipeline run scripts/fixtures/spring_signals \
            --compliance-profile certified \
            --allow-mock \
            --out-dir pipeline-artifacts \
            --skip-drift \
            --signals-file scripts/fixtures/spring_signals_fixture_expected.json

      - name: Gate certification.json (certified mock)
        run: doc-engine certification verify --allow-mock pipeline-artifacts/certification.json

  pr-comment:
    if: github.event_name == 'pull_request'
    needs: [certify-scan-only, certify-certified]
    runs-on: ubuntu-latest
    steps:
      - name: Comment certification result
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: "Pipeline certification passed (`scan_only` + `certified` mock profiles on fixture signals). See workflow logs for `pipeline-artifacts-scan/` and `pipeline-artifacts/` certification.json."
            });
